В рамках БД "Веб-магазин" (песочница) укажите запросы для каждой операции:

Создание таблицы с индексом "не пустой", "уникальный" для поля ID.
CREATE TABLE Users (
			id INTEGER NOT NULL UNIQUE,
            first_name VARCHAR(128),
            last_name VARCHAR(128)
            );

Вывод количества сотрудников.
SELECT COUNT (*) AS Total
FROM Employees;

Укажите разницу между CRUD и Aggregation в БД.
CRUD это основные функции для манипуляции данными, а Aggregation это сбор и подготовка данных для их дальнейшей обработки (вычислений).
